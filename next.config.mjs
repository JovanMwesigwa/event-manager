/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.lumacdn.com",
      "img.clerk.com",
      "images.unsplash.com",
      "images.lemonade.social",
      "pbs.twimg.com",
    ],
  },
};

export default nextConfig;
