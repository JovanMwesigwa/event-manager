export type EventActivityType = {
  id: number;
  title: string;
  description: string;
  host: string;
  start: string;
  end: string;
  duration: string;
  image: string;
  active: boolean;
  currentTime: string;
  done: boolean;
};
